/// Paths for the images declared under `flutter: assets:` in pubspec.yaml.
/// Not currently referenced by any widget — the app renders its wordmark as
/// text (see ArdiLogoCard's doc comment) rather than these PNGs.
class AppAssets {
  AppAssets._();

  static const greenLogo = 'assets/green_logo.png';
  static const whiteLogo = 'assets/white_logo.png';
}
